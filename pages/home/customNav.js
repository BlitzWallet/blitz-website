// const nav = document.querySelector("nav");

// function addNavStyle(e) {
//   const scrollHeight = window.scrollY;

//   nav.classList.remove(`${scrollHeight != 0 ? "stationaryNav" : "movingNav"}`);
//   nav.classList.add(`${scrollHeight === 0 ? "stationaryNav" : "movingNav"}`);

//   console.log(scrollHeight);
// }

// window.addEventListener("scroll", addNavStyle);
